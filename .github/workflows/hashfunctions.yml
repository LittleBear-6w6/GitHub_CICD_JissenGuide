name: Hash functions
on: push
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "${HASH}"
        env:
          HSH: ${{ hashFiles('.github/workflows/*.hml') }}
