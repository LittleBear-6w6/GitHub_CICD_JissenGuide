name: Hash functions
on: push
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actons/checkout@v4
      - run: echo "${HASH}"
        env:
          HSH: ${{ hashFiles('.github/workflows/*.hml') }}
